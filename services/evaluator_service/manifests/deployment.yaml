# Evaluator Service Deployment for OpenShift
# Evaluates RAG answer quality using LLM
#
# Prerequisites:
#   1. Create the secret with your actual API key BEFORE deploying:
#      oc create secret generic evaluator-service-secrets \
#        --from-literal=OPENAI_API_KEY="your-actual-api-key" \
#        -n advanced-rag
#
#   2. Or update existing secret:
#      oc create secret generic evaluator-service-secrets \
#        --from-literal=OPENAI_API_KEY="your-actual-api-key" \
#        -n advanced-rag --dry-run=client -o yaml | oc apply -f -
#
# Deploy:
#   oc apply -f services/evaluator_service/manifests/deployment.yaml -n advanced-rag
#
# NOTE: The Secret below is a TEMPLATE with placeholder values.
#       You MUST create/update the secret with real values before the service will work.
---
apiVersion: v1
kind: Secret
metadata:
  name: evaluator-service-secrets
  labels:
    app: evaluator-service
    app.kubernetes.io/name: evaluator-service
    app.kubernetes.io/part-of: advanced-rag
  annotations:
    description: "TEMPLATE - Replace OPENAI_API_KEY with actual value before deploying"
type: Opaque
stringData:
  # IMPORTANT: Replace this placeholder with your actual API key
  # See header comments for instructions
  OPENAI_API_KEY: "REPLACE_WITH_ACTUAL_API_KEY"
  # AUTH_TOKEN: "optional-auth-token"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: evaluator-service-config
  labels:
    app: evaluator-service
    app.kubernetes.io/name: evaluator-service
    app.kubernetes.io/part-of: advanced-rag
data:
  # LLM model for evaluation
  OPENAI_EVAL_MODEL: "gpt-4o-mini"
---
apiVersion: v1
kind: Service
metadata:
  name: evaluator-service
  labels:
    app: evaluator-service
    app.kubernetes.io/name: evaluator-service
    app.kubernetes.io/part-of: advanced-rag
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app: evaluator-service
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: evaluator-service
  labels:
    app: evaluator-service
    app.kubernetes.io/name: evaluator-service
    app.kubernetes.io/part-of: advanced-rag
  annotations:
    haproxy.router.openshift.io/timeout: 120s
spec:
  to:
    kind: Service
    name: evaluator-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: evaluator-service
  labels:
    app: evaluator-service
    app.kubernetes.io/name: evaluator-service
    app.kubernetes.io/part-of: advanced-rag
spec:
  replicas: 1
  selector:
    matchLabels:
      app: evaluator-service
  template:
    metadata:
      labels:
        app: evaluator-service
        app.kubernetes.io/name: evaluator-service
        app.kubernetes.io/part-of: advanced-rag
    spec:
      containers:
        - name: evaluator-service
          image: image-registry.openshift-image-registry.svc:5000/advanced-rag/evaluator-service:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: evaluator-service-config
            - secretRef:
                name: evaluator-service-secrets
          resources:
            requests:
              cpu: "100m"
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
      restartPolicy: Always

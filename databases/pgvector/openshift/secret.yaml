# PostgreSQL credentials
# IMPORTANT: Change these values before deploying to production!
---
apiVersion: v1
kind: Secret
metadata:
  name: pgvector-credentials
  labels:
    app: pgvector
    app.kubernetes.io/name: pgvector
    app.kubernetes.io/part-of: advanced-rag
type: Opaque
stringData:
  # Default development credentials - CHANGE FOR PRODUCTION
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "changeme-in-production"
  POSTGRES_DB: "vector"
  # Connection string for applications
  # Format: postgresql://user:password@host:port/database
  PGVECTOR_CONN: "postgresql://postgres:changeme-in-production@pgvector.advanced-rag.svc.cluster.local:5432/vector"

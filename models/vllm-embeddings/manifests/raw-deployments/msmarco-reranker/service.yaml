apiVersion: v1
kind: Service
metadata:
  name: msmarco-reranker
  namespace: vllm-embeddings
  labels:
    app: msmarco-reranker
spec:
  selector:
    app: msmarco-reranker
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

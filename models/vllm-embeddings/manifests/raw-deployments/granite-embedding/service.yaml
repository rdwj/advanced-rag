apiVersion: v1
kind: Service
metadata:
  name: granite-embedding
  namespace: vllm-embeddings
  labels:
    app: granite-embedding
spec:
  selector:
    app: granite-embedding
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

# HuggingFace Token Secret for Pyannote
#
# The pyannote speaker-diarization model requires accepting license terms:
# 1. Go to https://huggingface.co/pyannote/speaker-diarization-3.1
# 2. Accept the license terms
# 3. Create a token at https://huggingface.co/settings/tokens
#
# Create the secret using one of these methods:
#
# Method 1: Using oc command (recommended)
#   oc create secret generic pyannote-hf-token \
#     --from-literal=token=hf_your_token_here \
#     -n models
#
# Method 2: Using Makefile
#   HF_TOKEN=hf_your_token_here make create-secret NAMESPACE=models
#
# Method 3: Apply this file (after replacing the token)
#   cp secret.yaml.example secret.yaml
#   # Edit secret.yaml and replace YOUR_HF_TOKEN_HERE
#   oc apply -f secret.yaml -n models

apiVersion: v1
kind: Secret
metadata:
  name: pyannote-hf-token
  labels:
    app: pyannote-server
type: Opaque
stringData:
  token: YOUR_HF_TOKEN_HERE

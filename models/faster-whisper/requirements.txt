# FastAPI and web server
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# Faster-whisper for transcription
faster-whisper>=1.0.0

# PyTorch (CUDA 12.1 wheels installed via extra-index-url)
torch>=2.0.0

# Data validation
pydantic>=2.0.0

# Audio processing
soundfile>=0.12.0
av>=12.0.0  # PyAV provides ffmpeg bindings for audio format conversion

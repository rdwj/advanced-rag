apiVersion: v1
kind: Service
metadata:
  name: rerank-service
  labels:
    app: rerank-service
    app.kubernetes.io/name: rerank-service
    app.kubernetes.io/part-of: advanced-rag
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app: rerank-service

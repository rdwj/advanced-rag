apiVersion: v1
kind: Service
metadata:
  name: plan-service
  labels:
    app: plan-service
    app.kubernetes.io/name: plan-service
    app.kubernetes.io/part-of: advanced-rag
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app: plan-service
